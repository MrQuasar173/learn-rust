pub fn _make_new_franchise() {} // Nested mod to include files (via folders) (this is a child mod in mod
//national hq, which is brought into lib.rs)

// Child mods must be in a directory that is in the same directory as the parent mod, the directory must
// have the same name as the parent mod's name (filename), and the filename of the child mod must match the
// call in the parent mod.